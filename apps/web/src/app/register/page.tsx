import { RegisterForm } from './register-form';

export default function RegisterPage() {
  return (
    <main className="min-h-screen bg-canvas px-md py-section">
      <RegisterForm />
    </main>
  );
}
